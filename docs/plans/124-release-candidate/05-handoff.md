# Implementation Handoff

Implement only version and candidate-guide integration first. Promote this plan
to docs/releases/1.2.0-candidate.md, remove the temporary pack, reconcile exact head
and merge after self-review/checks. Keep issue124 and release issues open.

Then retain/verify/nominate and run exact-byte tests. Surface the acceptance packet
and any new authority needed; never invent a human verdict or silently widen old
candidate exceptions. Publication and live installation are separate operations.
No new dependencies, auth setup, personal migration or roadmap expansion.
