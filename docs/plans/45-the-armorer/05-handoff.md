# Handoff

Slices: alias tests/implementation; projection and packaging tests/context; skill and administrative routing; docs, native behavioral checks and rendered labels.

Promote durable guidance to docs/setup.md, docs/interface.md and the skill reference. Keep schemas authoritative in operations, not copied into the skill. Remove this temporary six-file plan in the reconciled implementation PR. Link #45 and affected #6/#7/#10.

Use contributor self-review honestly; required hosted checks remain required. Retest changed candidate surfaces, preserve old acceptance evidence and user fixture. No real-memory import, daemon, self-modifying toolchain, new harness, live secret enrollment or public release.

