# Verification

Failing first: CLI armorer/doctor output and error parity with explicit synthetic profile, no created state; generated context matching the exact plan including paths with quotes/spaces; both skills in packaged projection; administrative operations remain excluded from MCP. Then implement and run focused, full race/vet/build checks.

Native contributor tests in the designated pane: natural read-only diagnostic request with exact local context; no-bank/setup preview with no implicit writes; user-requested bounded administrative operation; unaffected passive memory routing. Retained artifacts and owner tests follow a newly nominated candidate, not the old digest.

UI classification: in-pattern-visual-change. Record root menu and diagnostic report at normal and narrow widths, plain/no-color keyboard routes, unchanged default-No repair. No browser surface, locale/screen-reader claim or new pixel baseline.

## Production Readiness Preflight
Execution envelope is implementation only. Public release is not authorized. Existing artifact pipeline must build a replacement candidate, then record candidate-specific authorization/acceptance and publish only upon explicit direction. Old evidence cannot approve new bytes.

