# Implementation Handoff

First failing-first renderer and inert-shell copy tests, then minimal literal
surface plus release receipt wiring, then actual terminal verification and CI.
Each issue criterion maps to exact-byte/shell tests or rendered evidence in
04-verification.md; neither substitutes for the other.

Promote copy instructions into the runbook/interface and evidence into a bounded
engineering evidence directory. Delete this temporary six-file pack during
exact-head reconciliation. Keep #77 open after merge for candidate acceptance
and release. Engineering review uses ADR0003, never claims independent approval.

No clipboard integration, generalized shell runner, arbitrary file creation,
retry policy changes or personal pending-state inspection. Reopen design if
literal output cannot preserve command meaning or requires another trust boundary.
