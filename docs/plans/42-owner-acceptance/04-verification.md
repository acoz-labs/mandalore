# Verification

Test-first actual shell execution covers default denial, exact success and mismatch in every selector. Exercise actual acceptance recorder with synthetic local gh responses: both author checks, unlisted owner, missing confirmation, unrelated reviewer normal path and retained downstream failure gates. No test calls GitHub or writes real acceptance. Full container-first CI, pinned host fallback if unavailable, hosted PR CI and merged-main checks remain required.

## Production Readiness Preflight
No production promotion authorized. Candidate payload stays byte-identical. Owner configuration is nonsecret repository administration; no credential enrollment. Existing immutable nomination, source ancestry, required checks, issue evidence and separate release authorization remain. Revoke by clearing the owner variable; retained receipts disclose past exception use.
