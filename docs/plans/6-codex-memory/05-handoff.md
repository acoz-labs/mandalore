# Implementation Handoff

This is a substantive native integration. Implement in order: bounded read-only
adapter/CLI; public scaffold/bridge/MCP wiring; implicit memory skill; native
discovery and behavior evidence. Connection menu/repair remain #7, migration #8,
retrieval evaluation #9 and cross-machine candidate acceptance #10.

Promote decisions and actual behavior into plugins/codex/README.md,
docs/interface.md, docs/development.md and docs/runbook.md; update attribution
and privacy checks for public source-derived files. Do not retain this six-file
plan after implementation reconciliation.

The planning PR is planning-only. Record exact-head self-review, then implement
on feature/codex-memory. Keep the implementation PR draft until tests, native
evidence, docs promotion and plan removal are reconciled to the actual head.
Keep #6 open for candidate acceptance even if implementation merges.

No custom credential provider, agent identity, general lifecycle subscription
bus, capability executor, transcript harvesting, native home replacement,
unattended trust acceptance or magic-phrase command parser. Return to design
only for a material trust/data boundary change, not ordinary engineering choices.
