# Verification

## Required matrix

| Group | Evidence |
| --- | --- |
| Current/history/aliases | Exact current IDs, explicit superseded exclusions, stable scope through rename, known alias versus absent synonym limitation |
| Isolation/ambiguity | Multiple projects sharing terms; project preference absent from unscoped recall; complete paged scope discovery without cwd guesses |
| Conflicts/no match/budgets | Conflict heads never current guidance; unrelated-word conflict still signaled within scope; honest counts/truncation; whole claims and bounded JSON |
| Freshness | Same service observes external valid additions/corrections/conflicts; replaced identity refuses; corrupt data is not silently omitted |
| Scale | Warm/fresh-service/compiled-process counts, history depth, query breadth, scopes/hooks and separate foundlings measurements; retained raw output |
| Native efficiency | Same synthetic task before/after any guidance change; correct scoped answer, provenance only when requested/needed, no-save preservation, ordered call ledger |
| Learning still works | Authorized native correction uses predecessor IDs and appropriate scope; a fresh read returns the correction and history |

Add characterization/evaluation assertions before changing behavior. An actual
defect requires a failing-first regression; an intentional lexical miss is labeled
as such rather than made into a failing semantic feature request. Benchmark harness
sanity checks verify fixture counts, correctness and result-size accounting before
trusting its timings. Avoid measuring fixture setup or cached test completion as
operation latency.

Run the full `bin/ci` through the supported container/host path in the designated
Herdr pane. Run opt-in measurements there with exact commands/versions. Validate
any native skill/plugin edits with their required development workflows before
installing a managed disposable test connection. Compare baseline/final on the same
host/fixture, report noisy samples and make no cross-machine performance inference.

## Rendered and native evidence

Classification: `no-rendered-impact` for evaluation code and bounded retrieval/
guidance changes; no menus or renderer are changed. Native behavioral output is
still required for guidance changes, with exact runtime/plugin identities, fixture
receipts, observed tool calls and preservation checks. If implementation actually
changes a user-owned rendered surface, define and run its UI matrix before readiness
instead of extending this exemption.

## Readiness and release

Promote findings/reproduction/known limitations to durable docs, reconcile the exact
head with this plan, remove all six temporary files, and record engineering self-review
under decision 0001. Hosted checks must pass the final reviewed commit. Keep #9 open
for independent candidate acceptance if release-bearing changes are included.

## Production Readiness Preflight

The execution envelope cannot touch production. There is no new secret slot,
service deployment, live migration, package install or public release. Candidate
build/nomination and independent acceptance remain #11/#10; native contributor
measurements are not a substitute. Existing local-first storage and plugin repair
paths remain intact. Rollback of code/guidance needs no bank rewrite because the
storage format is unchanged; preserve evidence and synthetic fixtures for diagnosis.
