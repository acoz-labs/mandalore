# Context

Source main is 55b1e89c6e8f9c18d5fa6814274df3e87d3db240. Reviewed implementations
for #56, #61, #65, #57, #13, #74 and #85 are merged; their artifact acceptance
remains open. VERSION still reads 1.0.0, whose published bytes are immutable.
The builder already stamps runtime/Codex/Pi in a private clean export, retaining
eight assets. Existing nomination and acceptance tools bind source and manifest.

Users need one testable upgrade, not separate mutable feature builds. No new
memory schema, runtime dependency, menu design or native authentication flow is
needed. This preparation has no rendered impact; candidate tests must still
exercise the included model-facing and terminal changes on exact new bytes.
