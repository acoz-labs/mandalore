# Handoff

Implement on feature/foundlings after recorded planning self-review under decision
0001. Start with pure registration routing/state tests, then bounded local/Git reads,
verified citation/promotion, shared operations and the native/menu adapters. Each
slice must preserve ordinary memory behavior and expose honest partial outcomes.

The smallest complete outcome includes both agent-ready access and management UX;
do not call a registration-only API or documentation-only workflow done. Source
verification is not semantic endorsement. Conversely, do not substitute bulk import
for useful consultation or require a confirmation ceremony for every ordinary fact.

Promote durable behavior to a focused docs/foundlings.md guide, signet-format and
interface contracts, setup/runbook, architecture/development and native skill/plugin
guidance. Record actual synthetic/native/UI evidence, exact tested artifacts and
review findings. Update the roadmap and #9/#10 acceptance links where needed.
Reconcile the exact draft implementation head with this plan, include a documentation
promotion matrix, then remove all six temporary plan files and the empty directory.

Preserve source origin attribution in NOTICE when adapting existing implementation.
Do not revive the predecessor capability-builder framework. Keep #12 open after
engineering merge for immutable-candidate acceptance; no private-source adoption,
auth setup, source deletion or GitHub Release is implied.

Reopen design only for a new source/execution/credential boundary, semantic-authority
change, irreversible migration or incompatible schema change. Ordinary implementation
decisions and in-pattern menu refinement remain delegated.
