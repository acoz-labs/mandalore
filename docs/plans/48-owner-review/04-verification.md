# Verification

Add failing-first predicate coverage for both allowed issue sets and mixed pairs,
wrong artifact/repo/issue/actor, missing or false confirmation and arity. Run
actual synthetic recorder tests against both candidate identities including #45.
Preserve no-writes-on-denial and normal independent-reviewer cases. Validate plans,
shell syntax and pinned host CI in the designated testing pane; use container first
when available. Required hosted CI must pass before merge. No rendered UI changes.

## Production Readiness Preflight

Not applicable: implementation-only maintainer control update. No product rebuild,
nomination, acceptance submission, publication, credentials or memory mutation.
Exact product bytes remain retained. Actual verdict, issue evidence and publication
authority must be handled separately after eligibility is enabled.
