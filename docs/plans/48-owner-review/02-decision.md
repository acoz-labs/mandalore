# Decision

Add one literal case to the existing fail-closed predicate. Retain old eligibility
for history without extending old scope to #45. A broad allowlist, ancestor-based
acceptance or unrestricted owner bypass would exceed authorization. No new
configuration or actor identifiers enter source. Revoke through the existing
owner variable or a scoped policy correction.
