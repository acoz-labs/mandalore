# Handoff

1. Record exact-head self-review of this returned design and pass required CI.
2. Make the two bounded guidance edits; preserve the original selector and wire.
3. Run the declared native matrix, skill/selector checks and full CI. Retain
   failures and stop expanding the approach if sequencing still fails.
4. Reconcile the exact implementation head; update `docs/interface.md` and the
   MCP evidence guide with sequencing, costs and limitations. Add a compact
   synthetic evidence receipt, not private sessions or workstation paths.
5. Remove this temporary plan before implementation leaves draft. Keep #56 and
   candidate integration #88 open through their distinct release gates.

Ordinary details and engineering self-review are delegated by ADR 0003. Changes
to protocol, trust, native global rendering or product acceptance policy are not
authorized by this plan. No additional implementation scope is implied.
